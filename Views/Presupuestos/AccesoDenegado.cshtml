@model dynamic
@{
    ViewData["Title"] = "Acceso Denegado";
}
<div class="row">
 <div class="col-md-10 mx-auto text-center">
  <h1 class="text-danger">❌ Acceso Denegado (403)</h1>
  <p>Su rol no tiene permisos para esta funcionalidad.</p>
  <div class="mt-3">
    <a asp-action="Index" class="btn btn-primary">Volver a Presupuestos</a>
    <a asp-controller="Login" asp-action="Logout" class="btn btn-secondary">Cerrar Sesión</a>
  </div>
 </div>
</div>